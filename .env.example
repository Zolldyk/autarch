# Autarch Environment Configuration
# Copy this file to .env and customize for your setup:
#   cp .env.example .env
#
# Then replace MASTER_SEED with your own BIP39 mnemonic and run:
#   npm start

# ─── Master Seed ───────────────────────────────────────────────────
# BIP39 mnemonic (space-separated words) OR hex string (64/128 chars).
# All agent wallets are derived from this single seed via HD derivation (BIP44).
#
# ⚠️  NEVER use the same seed for devnet and mainnet.
# ⚠️  Keep this secret — anyone with the seed controls all derived wallets.
#
# MASTER_SEED=your twelve or twenty-four word mnemonic goes here

# ─── Demo Mode ─────────────────────────────────────────────────────
# Set to "true" for zero-config demo with a built-in seed.
# FOR DEMO ONLY — DO NOT USE WITH REAL FUNDS.
# When MASTER_SEED is set, it takes priority over DEMO_MODE.
#
# DEMO_MODE=true

# ─── RPC Configuration ────────────────────────────────────────────
# Primary Solana RPC endpoint.
# Default: https://api.devnet.solana.com
#
# RPC_URL=https://api.devnet.solana.com

# Comma-separated fallback RPC endpoints (optional).
# When set, overrides RPC_URL with a rotation pool for resilience.
#
# RPC_ENDPOINTS=https://api.devnet.solana.com,https://devnet.helius-rpc.com/?api-key=YOUR_KEY

# ─── Dashboard ─────────────────────────────────────────────────────
# Port for the real-time dashboard server.
# Default: 3000
#
# PORT=3000
